# MAVIS — Agentic AI Capability Showcase

Single-file, self-contained interactive showcase (no build step, no dependencies).

**Live:** enable GitHub Pages on the `main` branch (root) and open the published URL.

`index.html` is the entire app.
